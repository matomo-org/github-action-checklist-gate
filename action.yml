name: 'Checklist Gate'
description: 'Fail pull requests when required checklist items are present but not checked'
author: 'Your Name'
inputs:
  checklist_lines:
    description: 'Newline-separated list of checklist item labels to enforce'
    required: true
runs:
  using: 'node20'
  main: 'dist/index.js'
